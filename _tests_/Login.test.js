// import { render } from '@testing-library/react-native';
// import React, { useContext } from 'react';
// import FirstScreen from '../screens/Login';

// jest.mock('react', () => ({
//   ...jest.requireActual('react'),
//   useContext: jest.fn(),
// }));

// test('renders correctly', () => {
//     useContext.mockImplementation(() => ({
//       apiKey: ,
//       token: ,
//     }));
  
//     const { getByText } = render(<FirstScreen />);
//     expect(getByText('API Key:')).toBeTruthy();
//     expect(getByText('Token:')).toBeTruthy();
//   });